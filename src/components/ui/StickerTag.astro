---
export interface Props { label: string; href?: string; }
const { label, href } = Astro.props;
---
{href ? <a class="sticker-tag" href={href}>{label}</a> : <span class="sticker-tag">{label}</span>}
