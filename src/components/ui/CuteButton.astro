---
export interface Props { href?: string; label: string; }
const { href, label } = Astro.props;
---
{href ? <a class="cute-button" href={href}>{label}</a> : <button class="cute-button" type="button">{label}</button>}
