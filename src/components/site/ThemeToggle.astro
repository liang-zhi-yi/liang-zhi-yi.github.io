<button class="theme-toggle" type="button" aria-label="切换深色模式" aria-pressed="false" data-theme-toggle>
  <span aria-hidden="true">☾</span>
</button>

<style>
  .theme-toggle { position: fixed; right: 18px; top: 100px; z-index: 40; width: 44px; height: 44px; border: 2px solid var(--border-soft); border-radius: 50%; background: var(--surface); color: var(--text-main); box-shadow: var(--shadow-card); font-size: 1.15rem; transition: transform .25s var(--ease-soft); }
  .theme-toggle:hover { transform: translateX(-3px) rotate(8deg); }
</style>
